server:
  port: 8099
  servlet:
    context-path: /demo
spring:
  datasource:
    url: jdbc:mysql://st01-build09.st01:3306/batchdb?characterEncoding=utf8&useSSL=true&serverTimezone=Hongkong
    username: root
    password: abcd1234
    driver-class-name: com.mysql.cj.jdbc.Driver
nutz:
  dao:
    runtime:
      create: false #自动创建表
      migration: false #根据bena自动更新表结构